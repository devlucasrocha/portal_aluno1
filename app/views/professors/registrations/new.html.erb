<div style="
  margin: auto;
  width: 50%;
  box-shadow: 5px 10px 8px #888888;
  border-radius: 5px;
  background-color: #d2d2d2;
  border-radius: 30px;
  padding: 20px;" class="img-fluid">
    <%= render 'buttonToClose' %>
<h2><%= t("devise_page.sign_up") %></h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>

  <form>
    <div class="form-group">
      <div class="field">
        <%= f.label :email %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"form-control" %>
      </div>
    </div>

    <div class="form-group">
        <div class="field">
          <%= t('devise_page.password') %>
          <% if @minimum_password_length %>
          <em>(<%= @minimum_password_length %> <%= t('devise_page.characters_minimum') %>)</em>
          <% end %><br />
          <%= f.password_field :password, autocomplete: "new-password", class:"form-control" %>
        </div>
    </div>

    <div class="form-group">
      <div class="field">
        <%= t('devise_page.password_confirmation') %><br />
        <%= f.password_field :password_confirmation, autocomplete: "new-password", class:"form-control" %>
      </div>
    </div>

    <div class="actions">
      <%= f.submit t('devise_page.sign_up_confirm'), class:"btn btn-primary" %>
    </div>
  <% end %>
<form>
<br>
<%= render "professors/shared/links" %>
<%= render 'languageSelect' %>
</div>